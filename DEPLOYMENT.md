# 🚀 Deployment Guide - RecipeGen Next.js

Полное руководство по деплою приложения RecipeGen на Vercel с Neon (PostgreSQL).

## 📋 Предварительные требования

1. **Аккаунты:**
   - [Vercel](https://vercel.com) (бесплатный план доступен)
   - [Neon](https://neon.tech) (бесплатный план доступен) - PostgreSQL database
   - [Google Cloud Console](https://console.cloud.google.com) (для OAuth)
   - [OpenAI](https://platform.openai.com) (для API ключа)
   - [TransferMit](https://transfermit.com) (для платежей)

2. **Локальная подготовка:**
   - Git репозиторий настроен
   - Проект компилируется без ошибок (`npm run build`)
   - Все тесты пройдены (если есть)

## 🔧 Шаг 1: Настройка Neon (База данных PostgreSQL)

### 1.1 Создание базы данных

1. Войдите в [Neon Dashboard](https://console.neon.tech)
2. Создайте новый проект:
   - Нажмите "Create Project"
   - Имя: `recipegen_prod` (или любое другое)
   - Регион: выберите ближайший
   - План: Free (для начала)

### 1.2 Получение connection string

1. После создания проекта, откройте его
2. Перейдите в "Connection Details"
3. Скопируйте connection string (формат: `postgresql://...`)
4. **Важно:** Используйте connection string с `?sslmode=require` для безопасности

### 1.3 Настройка Prisma для Neon

Prisma схема уже настроена для PostgreSQL. Убедитесь, что в `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

### 1.4 Применение схемы

```bash
# Генерируем Prisma Client
npm run db:generate

# Применяем схему к Neon
npm run db:push
```

**Важно:** Neon использует стандартный PostgreSQL, поэтому `relationMode` не требуется.

## 🌐 Шаг 2: Настройка Vercel

### 2.1 Подключение репозитория

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите "Add New Project"
3. Подключите ваш Git репозиторий (GitHub/GitLab/Bitbucket)
4. Выберите проект `forex_crypto`

### 2.2 Настройка проекта

Vercel автоматически определит Next.js. Убедитесь, что:
- **Framework Preset:** Next.js
- **Root Directory:** `./` (или оставьте пустым)
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию)
- **Install Command:** `npm install` (по умолчанию)

### 2.3 Настройка переменных окружения

В разделе "Environment Variables" добавьте все необходимые переменные:

#### Обязательные переменные:

```bash
# Database (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:5432/database?sslmode=require

# NextAuth
NEXTAUTH_SECRET=your-secret-here  # Сгенерируйте: openssl rand -base64 32
NEXTAUTH_URL=https://yourdomain.vercel.app  # Или ваш кастомный домен

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OpenAI
OPENAI_API_KEY=sk-...

# TransferMit
TM_API_KEY=your-transfermit-api-key
TM_SIGNING_KEY=your-transfermit-signing-key

# SMTP Email
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USER=your-email@domain.com
SMTP_PASS=your-email-password
SMTP_FROM=your-email@domain.com
SMTP_FROM_NAME=RecipeGen Support
```

#### Опциональные переменные:

```bash
SITE_BASE_URL=https://yourdomain.vercel.app
TM_API_URL=https://app.transfermit.com/api/v1
PUPPETEER_EXECUTABLE_PATH=  # Путь к Chromium (только для локальной разработки)
```

**Важно:** 
- Добавьте переменные для всех окружений (Production, Preview, Development)
- Для Production используйте production значения
- Для Preview можно использовать тестовые значения

### 2.4 Настройка Google OAuth

1. В [Google Cloud Console](https://console.cloud.google.com):
   - Создайте проект (если нет)
   - Включите "Google+ API"
   - Создайте OAuth 2.0 credentials
   - Добавьте authorized redirect URIs:
     - `https://yourdomain.vercel.app/api/auth/callback/google`
     - `https://yourdomain.vercel.app` (для локальной разработки)

2. Скопируйте `Client ID` и `Client Secret` в Vercel environment variables

### 2.5 Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки
3. После успешного деплоя, Vercel предоставит URL: `https://yourproject.vercel.app`

## 🔍 Шаг 3: Проверка после деплоя

### 3.1 Проверка работоспособности

1. Откройте ваш Vercel URL
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ Страница регистрации работает
   - ✅ Страница входа работает
   - ✅ Google OAuth работает (если настроен)

### 3.2 Тестирование функций

1. **Регистрация:**
   - Создайте тестовый аккаунт
   - Проверьте, что пользователь создан в Neon

2. **Пополнение баланса:**
   - Перейдите на `/top-up`
   - Инициируйте тестовый платеж
   - Проверьте webhook обработку

3. **Генерация рецепта:**
   - Пополните баланс
   - Сгенерируйте рецепт
   - Проверьте, что PDF и изображение созданы

### 3.3 Проверка логов

В Vercel Dashboard → ваш проект → "Logs":
- Проверьте отсутствие ошибок
- Убедитесь, что API routes работают корректно

## 🔐 Шаг 4: Настройка кастомного домена (опционально)

1. В Vercel Dashboard → ваш проект → "Settings" → "Domains"
2. Добавьте ваш домен
3. Следуйте инструкциям для настройки DNS
4. Обновите `NEXTAUTH_URL` в environment variables на новый домен
5. Обновите Google OAuth redirect URIs

## 📊 Шаг 5: Мониторинг и оптимизация

### 5.1 Vercel Analytics

Включите Vercel Analytics для отслеживания производительности:
- Dashboard → ваш проект → "Analytics"
- Включите Web Vitals и Real User Monitoring

### 5.2 Neon Insights

В Neon Dashboard:
- Мониторьте использование БД
- Проверяйте медленные запросы
- Оптимизируйте индексы при необходимости

### 5.3 Error Tracking

Рекомендуется подключить:
- [Sentry](https://sentry.io) для отслеживания ошибок
- [LogRocket](https://logrocket.com) для session replay

## 🐛 Решение проблем

### Проблема: Ошибка подключения к БД

**Решение:**
- Проверьте `DATABASE_URL` в Vercel environment variables
- Убедитесь, что Neon база активна
- Проверьте, что connection string содержит `?sslmode=require`
- Убедитесь, что `provider = "postgresql"` в `schema.prisma`

### Проблема: NextAuth не работает

**Решение:**
- Проверьте `NEXTAUTH_SECRET` (должен быть минимум 32 символа)
- Убедитесь, что `NEXTAUTH_URL` совпадает с вашим доменом
- Проверьте Google OAuth credentials

### Проблема: PDF генерация не работает

**Решение:**
- Убедитесь, что используется `@sparticuz/chromium` (уже настроено)
- Проверьте, что функция имеет достаточно памяти (3008 MB в `vercel.json`)
- Проверьте timeout (60 секунд в `vercel.json`)

### Проблема: Изображения не загружаются

**Решение:**
- Проверьте `next.config.js` → `images.remotePatterns`
- Убедитесь, что OpenAI DALL-E URLs разрешены
- Проверьте права доступа к `public/images/generated/`

## 📝 Чеклист перед продакшеном

- [ ] Все environment variables настроены в Vercel
- [ ] Neon база данных создана и схема применена
- [ ] Google OAuth настроен с правильными redirect URIs
- [ ] Тестовый деплой успешен
- [ ] Все основные функции протестированы
- [ ] Кастомный домен настроен (если нужен)
- [ ] SSL сертификат активен (автоматически в Vercel)
- [ ] Мониторинг настроен
- [ ] Backup стратегия для БД (Neon автоматически делает backups)

## 🔄 Обновление приложения

После каждого изменения в коде:

1. Закоммитьте изменения в Git
2. Запушьте в основную ветку
3. Vercel автоматически задеплоит изменения
4. Проверьте логи на наличие ошибок

Для ручного деплоя:
```bash
vercel --prod
```

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Neon Documentation](https://neon.tech/docs)
- [Next.js Deployment](https://nextjs.org/docs/deployment)
- [NextAuth.js Deployment](https://next-auth.js.org/configuration/options)

---

**Готово!** Ваше приложение должно быть доступно по адресу `https://yourproject.vercel.app`

