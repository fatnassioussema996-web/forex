# 🚀 Production Deployment Checklist

## ✅ Предварительная подготовка

### 1. База данных (Neon PostgreSQL)

**Важно:** Для Neon используйте connection string с connection pooling:
- Обычный connection string: `postgresql://user:pass@host/db?sslmode=require`
- С pooling (рекомендуется для Vercel): `postgresql://user:pass@host/db?sslmode=require&pgbouncer=true`

**Применение схемы к production БД:**

```bash
# Убедитесь, что DATABASE_URL указывает на production БД
# Затем примените схему:
npm run db:push

# ИЛИ создайте миграцию и примените её:
npm run db:migrate
# Затем для production:
npm run db:migrate:deploy
```

### 2. Переменные окружения в Vercel

**Обязательные переменные:**
- ✅ `DATABASE_URL` - connection string к Neon PostgreSQL (с pooling для serverless)
- ✅ `NEXTAUTH_SECRET` - минимум 32 символа (сгенерировать: `openssl rand -base64 32`)
- ✅ `NEXTAUTH_URL` - URL вашего Vercel домена (например: `https://yourproject.vercel.app`)

**Опциональные (для Google OAuth):**
- ⏳ `GOOGLE_CLIENT_ID` - будет добавлен позже
- ⏳ `GOOGLE_CLIENT_SECRET` - будет добавлен позже

**Как добавить в Vercel:**
1. Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Добавьте переменные для всех окружений (Production, Preview, Development)
3. После добавления переменных выполните передеплой

### 3. Миграции базы данных

**Для первого деплоя:**
```bash
# Локально (если еще не создана миграция):
npm run db:migrate

# Затем примените к production БД:
# Установите DATABASE_URL на production БД в .env
npm run db:migrate:deploy
```

**Или используйте db:push (быстрее для первого деплоя):**
```bash
# Убедитесь, что DATABASE_URL указывает на production БД
npm run db:push
```

### 4. Проверка перед деплоем

```bash
# Проверка сборки
npm run build

# Проверка линтера
npm run lint

# Проверка переменных окружения
npm run check-env
```

## 🚀 Деплой на Vercel

### Вариант 1: Через GitHub (рекомендуется)

1. Закоммитьте и запушьте изменения:
```bash
git add .
git commit -m "feat: add FAQ page and improve auth error handling"
git push origin main
```

2. Vercel автоматически задеплоит изменения

3. После деплоя проверьте:
   - Приложение открывается
   - Регистрация работает
   - Авторизация работает
   - Страницы загружаются

### Вариант 2: Через Vercel CLI

```bash
# Если еще не авторизованы
vercel login

# Деплой в production
vercel --prod
```

## ✅ После деплоя

1. **Проверьте работу регистрации:**
   - Откройте `/register`
   - Создайте тестовый аккаунт
   - Убедитесь, что пользователь создан в БД

2. **Проверьте работу авторизации:**
   - Войдите с созданным аккаунтом
   - Проверьте, что сессия создается

3. **Проверьте работу страниц:**
   - `/` - главная
   - `/courses` - список курсов
   - `/faq` - FAQ страница
   - `/pricing` - страница цен
   - `/dashboard` - дашборд (требует авторизации)

4. **Проверьте логи в Vercel:**
   - Vercel Dashboard → ваш проект → Logs
   - Убедитесь, что нет критических ошибок

## 🔧 Troubleshooting

### Проблема: "Can't reach database"
- Проверьте `DATABASE_URL` в Vercel environment variables
- Убедитесь, что connection string правильный
- Проверьте, что Neon БД активна

### Проблема: "NEXTAUTH_SECRET is missing"
- Убедитесь, что `NEXTAUTH_SECRET` добавлен в Vercel
- Проверьте, что секрет минимум 32 символа

### Проблема: "NEXTAUTH_URL mismatch"
- Убедитесь, что `NEXTAUTH_URL` совпадает с вашим Vercel доменом
- Проверьте, что URL начинается с `https://`

### Проблема: Миграции не применяются
- Выполните `npm run db:migrate:deploy` локально с production DATABASE_URL
- Или используйте `npm run db:push` для быстрого применения схемы

## 📝 Следующие шаги

После успешного деплоя:
1. Получите `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` из Google Cloud Console
2. Добавьте их в Vercel environment variables
3. Передеплойте проект
4. Протестируйте Google OAuth авторизацию

