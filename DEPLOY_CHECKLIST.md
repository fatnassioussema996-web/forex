# ✅ Production Deployment Checklist

## 🎯 Статус готовности

### ✅ База данных
- [x] **Миграции применены**: `npx prisma migrate status` показывает "Database schema is up to date!"
- [x] **Connection pooling настроен**: DATABASE_URL использует Neon pooler (`-pooler` в URL)
- [x] **Все таблицы созданы**: User, Session, Account, Course, Recipe, Topup, TransferMitTopup

### ✅ Переменные окружения в Vercel
- [x] `DATABASE_URL` - настроен (Neon PostgreSQL с pooling)
- [x] `NEXTAUTH_SECRET` - настроен (минимум 32 символа)
- [x] `NEXTAUTH_URL` - настроен (URL вашего Vercel домена)
- [ ] `GOOGLE_CLIENT_ID` - будет добавлен позже (опционально)
- [ ] `GOOGLE_CLIENT_SECRET` - будет добавлен позже (опционально)

### ✅ Код
- [x] Обработка ошибок БД в auth-функциях
- [x] Валидация email в регистрации
- [x] Connection pooling для Prisma настроен
- [x] Все переводы добавлены (FAQ, Pricing, Dashboard, Auth)
- [x] Линтер не показывает ошибок

## 🚀 Шаги для деплоя на Vercel

### 1. Push на GitHub
```bash
git add .
git commit -m "feat: add FAQ page, improve auth error handling, add email validation"
git push origin main
```

### 2. Vercel автоматически задеплоит
- Vercel автоматически обнаружит push
- Выполнит `npm run build`
- Применит переменные окружения
- Запустит `postinstall` скрипт (который выполнит `prisma generate`)

### 3. Проверка после деплоя
1. Откройте ваш Vercel домен
2. Проверьте страницу `/faq` - должна работать
3. Проверьте регистрацию - должна работать с валидацией email
4. Проверьте авторизацию - должна работать
5. Проверьте страницу `/courses` - должна загружаться

### 4. Если что-то не работает
- Проверьте логи в Vercel Dashboard → Deployments → [ваш деплой] → Functions
- Убедитесь, что все переменные окружения установлены
- Проверьте, что DATABASE_URL использует pooling connection string

## 📝 Важные заметки

### Connection Pooling для Neon
Ваш DATABASE_URL должен выглядеть так:
```
postgresql://user:pass@ep-xxx-pooler.region.aws.neon.tech/db?sslmode=require
```
Обратите внимание на `-pooler` в hostname - это важно для serverless функций Vercel.

### Google OAuth (опционально)
Если планируете использовать Google OAuth:
1. Создайте проект в Google Cloud Console
2. Настройте OAuth 2.0 credentials
3. Добавьте `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` в Vercel
4. Добавьте authorized redirect URI: `https://yourdomain.vercel.app/api/auth/callback/google`

### Миграции БД
Миграции уже применены к вашей production БД. При будущих изменениях схемы:
```bash
# Локально создайте миграцию:
npm run db:migrate

# Затем примените к production:
npm run db:migrate:deploy
```

## ✅ Готово к деплою!

Все критические компоненты проверены и готовы к production.

