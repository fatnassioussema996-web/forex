# 🚀 Быстрый деплой на Vercel

## Вариант 1: Через Vercel CLI (рекомендуется)

### Шаг 1: Применить Prisma схему к Neon БД

```bash
npm run db:push
```

### Шаг 2: Деплой через CLI

```bash
# Если еще не авторизованы
vercel login

# Первый деплой (создаст проект)
vercel

# Или деплой в production сразу
vercel --prod
```

### Шаг 3: Добавить Environment Variables

После первого деплоя через CLI, добавьте переменные через Dashboard или CLI:

```bash
# Через CLI (пример)
vercel env add DATABASE_URL production
vercel env add NEXTAUTH_SECRET production
# ... и так далее для всех переменных
```

Или через Dashboard:
1. Vercel Dashboard → ваш проект → Settings → Environment Variables
2. Добавьте все переменные из `ENV_VARIABLES.md`

### Шаг 4: Передеплой после добавления переменных

```bash
vercel --prod
```

---

## Вариант 2: Через Vercel Dashboard

### Шаг 1: Применить Prisma схему к Neon БД

```bash
npm run db:push
```

### Шаг 2: Подключить Git репозиторий

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите "Add New Project"
3. Подключите ваш Git репозиторий (GitHub/GitLab/Bitbucket)
4. Выберите проект `forex_crypto`

### Шаг 3: Настроить Environment Variables

В разделе "Environment Variables" добавьте все переменные из `ENV_VARIABLES.md`

**Важно:** Добавьте переменные для всех окружений (Production, Preview, Development)

### Шаг 4: Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки
3. После успешного деплоя получите URL: `https://yourproject.vercel.app`

---

## ⚠️ Важные моменты перед деплоем

1. **Применить Prisma схему:**
   ```bash
   npm run db:push
   ```

2. **Сгенерировать NEXTAUTH_SECRET:**
   ```bash
   openssl rand -base64 32
   ```

3. **Обновить NEXTAUTH_URL:**
   - После деплоя получите URL от Vercel
   - Обновите `NEXTAUTH_URL` в environment variables на этот URL
   - Обновите Google OAuth redirect URIs

4. **Проверить DATABASE_URL:**
   - Убедитесь, что connection string содержит `?sslmode=require`
   - Проверьте, что Neon БД активна

---

## 📋 Минимальный набор Environment Variables для первого деплоя

Для того, чтобы проект просто загрузился, нужны минимум:

```bash
DATABASE_URL=postgresql://... (ваш Neon connection string)
NEXTAUTH_SECRET=... (сгенерированный секрет)
NEXTAUTH_URL=https://yourproject.vercel.app (после первого деплоя)
```

Остальные переменные можно добавить позже, когда будут готовы.

---

## ✅ После деплоя проверьте

1. Главная страница загружается
2. TradingView виджеты работают
3. Переключение языков работает
4. Footer и Header отображаются
5. Нет ошибок в консоли браузера
6. Нет ошибок в Vercel Logs

---

**Готово к деплою!** 🚀

