# 🛠️ Финальный стек технологий для Next.js миграции

## 📋 Текущий стек (PHP)
- **Backend**: PHP 8.x, MySQL (mysqli)
- **Frontend**: Vanilla JS, Tailwind CSS 3.4
- **AI**: OpenAI API (GPT-4o, DALL-E 3)
- **Auth**: Google OAuth 2.0, PHP Sessions
- **PDF**: mPDF 6.1
- **Email**: PHPMailer (SMTP)
- **Payments**: TransferMit Gateway
- **Database**: MySQL 5.7+

---

## 🎯 Рекомендуемый стек для Next.js

### **Core Framework**
```json
{
  "next": "^14.2.0",           // App Router (рекомендуется) или Pages Router
  "react": "^18.3.0",
  "react-dom": "^18.3.0",
  "typescript": "^5.4.0"        // Строгая типизация
}
```

**Выбор**: **Next.js 14 App Router** (современный, лучшая производительность, Server Components)

---

### **Database & ORM**
```json
{
  "prisma": "^5.15.0",          // ORM (типобезопасность, миграции)
  "@prisma/client": "^5.15.0",
  "pg": "^8.11.3",              // PostgreSQL драйвер
  "@types/pg": "^8.10.9"        // TypeScript типы для pg
}
```

**Стратегия БД**:
- **Локально**: PostgreSQL (локальная установка или Neon)
- **Продакшн**: **Neon** (serverless PostgreSQL на Vercel)
- **Prisma** работает с PostgreSQL везде

**Почему PostgreSQL (Neon) для этого проекта**:
- ✅ Отличная поддержка Prisma
- ✅ Neon - лучший serverless PostgreSQL для Vercel
- ✅ Бесплатный tier Neon (0.5GB storage, 192MB RAM)
- ✅ Автоматические backups
- ✅ Отличная интеграция с Vercel
- ✅ Поддержка SSL из коробки

**Альтернативы PostgreSQL хостинга**:
- **Neon** ⭐ (рекомендуется) - serverless, отличная интеграция с Vercel
- **Supabase** - хороший вариант, но больше функций чем нужно
- **Railway PostgreSQL** - простой, но менее оптимизирован для serverless
- **AWS RDS PostgreSQL** - больше контроля, но сложнее настройка

**Рекомендация**: **PostgreSQL + Neon** (serverless, бесплатный tier, отличная интеграция)

---

### **Authentication**
```json
{
  "next-auth": "^4.24.0",      // или Auth.js v5 (beta)
  "@auth/prisma-adapter": "^1.0.0"
}
```

**Провайдеры**:
- Email/Password (Credentials)
- Google OAuth 2.0

**Альтернативы**:
- **Clerk** (SaaS, проще, но платно)
- **Supabase Auth** (если переходим на Supabase)

**Рекомендация**: **NextAuth.js v4** (стабильный, бесплатный, хорошо интегрируется)

---

### **Session Management**
```json
{
  "next-auth": "^4.24.0"        // Встроенные сессии через JWT или database sessions
}
```

**Стратегия**: Database sessions (Prisma adapter) для безопасности и контроля

---

### **AI Integration**
```json
{
  "openai": "^4.28.0"           // Официальный OpenAI SDK для Node.js
}
```

**Использование**:
- GPT-4o для генерации рецептов
- DALL-E 3 для изображений

---

### **PDF Generation**
```json
{
  "puppeteer": "^22.0.0",       // Генерация PDF из HTML
  "react-pdf": "^8.0.0"         // Альтернатива: генерация из React компонентов
}
```

**Альтернативы**:
- **jsPDF** (легче, но менее гибкий)
- **pdfkit** (низкоуровневый контроль)

**Рекомендация**: **Puppeteer** (лучшая совместимость с HTML/CSS, как mPDF)

---

### **Email**
```json
{
  "nodemailer": "^6.9.0",       // SMTP клиент для Node.js (аналог PHPMailer)
  "@types/nodemailer": "^6.4.0"
}
```

**Конфигурация**: SMTP (Namecheap Private Email)

---

### **Forms & Validation**
```json
{
  "react-hook-form": "^7.51.0",  // Управление формами
  "zod": "^3.22.0",              // Валидация схем
  "@hookform/resolvers": "^3.3.0" // Интеграция Zod с React Hook Form
}
```

**Рекомендация**: **React Hook Form + Zod** (типобезопасность, производительность)

---

### **Styling**
```json
{
  "tailwindcss": "^3.4.1",      // Уже используется
  "autoprefixer": "^10.4.0",
  "postcss": "^8.4.0"
}
```

**Конфигурация**: Сохранить текущий `tailwind.config.js`

---

### **State Management**
```json
{
  "zustand": "^4.5.0"           // Легковесный state management
}
```

**Альтернативы**:
- **Context API** (встроенный, для простых случаев)
- **Jotai** (атомарный state)
- **Redux Toolkit** (если нужна сложная логика)

**Рекомендация**: **Zustand** (простой, производительный, TypeScript-friendly)

---

### **HTTP Client**
```json
{
  "axios": "^1.6.0"              // Или встроенный fetch (Next.js 14)
}
```

**Рекомендация**: Встроенный **fetch** (Next.js оптимизирует автоматически)

---

### **Environment Variables**
```json
{
  // Использовать .env.local, .env.production
  // Next.js встроенная поддержка
}
```

**Структура**:
```
.env.local          # Локальная разработка
.env.production     # Продакшн
.env.example        # Шаблон (без секретов)
```

---

### **File Storage**
```json
{
  // Локальное хранение файлов в public/ директории
  // PDF и изображения сохраняются в public/recipes/generated/ и public/images/generated/
}
```

**Стратегия**:
- **Локально**: Файловая система (`public/recipes/generated/`, `public/images/generated/`)
- **Продакшн**: Файловая система Vercel (`public/` директория доступна через CDN)

**Примечание**:
- PDF и изображения хранятся локально в `public/` директории
- Vercel автоматически раздает файлы из `public/` через CDN
- Для больших объемов можно использовать Neon Storage или другие решения в будущем

**Рекомендация**: **Локальное хранение** (простота, достаточное для текущих потребностей)

---

### **Payment Gateway**
```json
{
  // TransferMit API (REST)
  // Использовать встроенный fetch или axios
}
```

**Интеграция**: Прямые HTTP запросы к TransferMit API (как сейчас)

---

### **Development Tools**
```json
{
  "@types/node": "^20.11.0",
  "@types/react": "^18.2.0",
  "@types/react-dom": "^18.2.0",
  "eslint": "^8.57.0",
  "eslint-config-next": "^14.2.0",
  "prettier": "^3.2.0"
}
```

---

### **Deployment**
```json
{
  // Vercel (рекомендуется для Next.js)
  // PlanetScale MySQL (через Vercel интеграцию)
  // Vercel Blob Storage (для файлов)
}
```

**Стек деплоя**:
- **Hosting**: Vercel (Next.js оптимизация из коробки)
- **Database**: Neon PostgreSQL (serverless, автоматическое масштабирование)
- **Storage**: Локальное хранение в `public/` (CDN через Vercel)
- **Environment**: Автоматические переменные окружения через Vercel Dashboard

**Преимущества Neon**:
- ✅ Serverless PostgreSQL (автоматическое масштабирование)
- ✅ Автоматические backups
- ✅ Бесплатный tier (0.5GB storage, 192MB RAM)
- ✅ Отличная интеграция с Vercel
- ✅ Prisma поддерживает из коробки
- ✅ SSL из коробки

**Альтернативы**:
- **Railway MySQL** - проще, но менее оптимизирован
- **AWS RDS** - больше контроля, но сложнее

**Преимущества Vercel**:
- Бесплатный tier для старта
- Автоматические деплои из Git
- Edge Network для быстрой загрузки
- Интеграция PlanetScale и Blob Storage из коробки

---

## 📦 Полный package.json

```json
{
  "name": "recipegen-nextjs",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:studio": "prisma studio"
  },
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "next-auth": "^4.24.0",
    "@auth/prisma-adapter": "^1.0.0",
    "@prisma/client": "^5.15.0",
    "prisma": "^5.15.0",
    "pg": "^8.11.3",
    "@types/pg": "^8.10.9",
    "framer-motion": "^11.0.5",
    "openai": "^4.28.0",
    "puppeteer": "^22.0.0",
    "nodemailer": "^6.9.0",
    "react-hook-form": "^7.51.0",
    "zod": "^3.22.0",
    "@hookform/resolvers": "^3.3.0",
    "zustand": "^4.5.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.11.0",
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "@types/nodemailer": "^6.4.0",
    "tailwindcss": "^3.4.1",
    "autoprefixer": "^10.4.0",
    "postcss": "^8.4.0",
    "eslint": "^8.57.0",
    "eslint-config-next": "^14.2.0",
    "prettier": "^3.2.0"
  }
}
```

---

## 🔄 Миграция компонентов

### **PHP → Next.js маппинг**

| PHP | Next.js |
|-----|---------|
| `index.php` | `app/page.tsx` |
| `login.php` | `app/login/page.tsx` |
| `register.php` | `app/register/page.tsx` |
| `cabinet.php` | `app/cabinet/page.tsx` |
| `handle_generation.php` | `app/api/generation/route.ts` |
| `api/topup-init.php` | `app/api/topup/init/route.ts` |
| `templates/header.php` | `components/Header.tsx` |
| `templates/footer.php` | `components/Footer.tsx` |
| `currency-utils.php` | `lib/currency-utils.ts` |
| `config.php` | `.env.local` + `lib/config.ts` |

---

## ✅ Преимущества выбранного стека

1. **TypeScript** - типобезопасность, меньше багов
2. **Prisma** - автоматические типы из схемы БД
3. **NextAuth.js** - готовая аутентификация
4. **Server Components** - лучшая производительность
5. **App Router** - современный роутинг, layouts
6. **Puppeteer** - надежная генерация PDF
7. **Zod** - валидация на клиенте и сервере

---

## ⚠️ Важные решения

### 1. **App Router vs Pages Router**
✅ **App Router** - современный, Server Components, лучшая производительность

### 2. **Prisma vs Drizzle vs Kysely**
✅ **Prisma** - лучшая документация, миграции, типобезопасность

### 3. **NextAuth vs Clerk**
✅ **NextAuth** - бесплатный, гибкий, хорошо интегрируется

### 4. **Puppeteer vs jsPDF**
✅ **Puppeteer** - лучшая совместимость с HTML/CSS (как mPDF)

### 5. **Zustand vs Context API**
✅ **Zustand** - простой, производительный, TypeScript-friendly

---

## 🚀 Следующие шаги

1. ✅ Создать Next.js 14 проект с TypeScript (App Router)
2. ✅ Настроить Prisma с MySQL (локально)
3. ✅ Мигрировать схемы БД из MySQL в Prisma
4. ✅ Настроить NextAuth.js (Email/Password + Google OAuth)
5. ✅ Перенести компоненты (Header, Footer, страницы)
6. ✅ Создать API routes (generation, topup, webhook)
7. ✅ Настроить PDF генерацию (Puppeteer)
8. ✅ Интегрировать OpenAI (GPT-4o, DALL-E 3)
9. ✅ Настроить email отправку (Nodemailer)
10. ✅ Локальное тестирование
11. 🔄 Настроить PlanetScale MySQL (создать БД, получить connection string)
12. 🔄 Настроить Vercel Blob Storage
13. 🔄 Деплой на Vercel
14. 🔄 Миграция данных из текущего MySQL в PlanetScale

---

## 📝 Важные заметки

### **Миграция БД: MySQL → PlanetScale**

**Подход**:
1. Начать с MySQL локально (текущая БД)
2. Создать Prisma схему на основе текущих таблиц
3. После деплоя на Vercel настроить PlanetScale MySQL
4. Использовать Prisma Migrate для создания схемы в PlanetScale
5. Экспортировать данные из текущего MySQL и импортировать в PlanetScale

**Prisma Schema пример**:
```prisma
// schema.prisma
datasource db {
  provider = "mysql"        // И локально, и продакшн (PlanetScale)
  url      = env("DATABASE_URL")
  relationMode = "prisma"   // Для PlanetScale (serverless)
}
```

**Преимущества PlanetScale**:
- ✅ Та же БД (MySQL) - нет необходимости менять SQL запросы
- ✅ Serverless - автоматическое масштабирование
- ✅ Branching - можно тестировать миграции на dev ветке
- ✅ Бесплатный tier достаточен для старта

**Миграция данных**:
```bash
# Экспорт из текущего MySQL
mysqldump -u user -p database_name > backup.sql

# Импорт в PlanetScale (через PlanetScale CLI или Dashboard)
pscale connect database_name --execute "source backup.sql"
```

---

## ✅ Финальные решения

1. ✅ **Next.js 14 App Router** - современный, Server Components
2. ✅ **Prisma** - ORM с поддержкой PostgreSQL
3. ✅ **Vercel** - хостинг с оптимизацией Next.js
4. ✅ **Neon PostgreSQL** - serverless PostgreSQL на Vercel
5. ✅ **Локальное хранение** - файлы в `public/` директории (CDN через Vercel)
6. ✅ **NextAuth.js** - аутентификация
7. ✅ **Puppeteer** - генерация PDF
8. ✅ **React Hook Form + Zod** - формы и валидация
9. ✅ **Zustand** - state management
10. ✅ **Framer Motion** - анимации

---

## 🆚 PostgreSQL (Neon) - Выбранное решение

### **Почему PostgreSQL с Neon:**

| Критерий | PostgreSQL + Neon |
|----------|-------------------|
| **Serverless на Vercel** | ✅ Отличная интеграция |
| **Бесплатный tier** | ✅ 0.5GB storage, 192MB RAM |
| **Автоматические backups** | ✅ Включены |
| **SSL поддержка** | ✅ Из коробки |
| **Prisma поддержка** | ✅ Отлично |
| **Масштабирование** | ✅ Автоматическое |
| **Производительность** | ✅ Оптимизировано для serverless |

**Вывод**: PostgreSQL + Neon - оптимальный выбор для serverless Next.js приложения!

