# 🚀 Quick Vercel Deployment Guide

Краткая инструкция по деплою проекта Avenqor на Vercel.

## 📋 Предварительные требования

- ✅ Neon БД создана и подключена
- ✅ DATABASE_URL обновлен в локальном .env
- ✅ Git репозиторий настроен

## 🔧 Шаг 1: Подготовка проекта

### 1.1 Применение Prisma схемы к Neon

```bash
# Генерируем Prisma Client
npm run db:generate

# Применяем схему к Neon БД
npm run db:push
```

### 1.2 Проверка билда локально (опционально)

```bash
npm run build
```

## 🌐 Шаг 2: Деплой на Vercel

### 2.1 Подключение репозитория

1. Войдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Нажмите "Add New Project"
3. Подключите ваш Git репозиторий
4. Выберите проект `forex_crypto`

### 2.2 Настройка проекта

Vercel автоматически определит Next.js. Убедитесь, что:
- **Framework Preset:** Next.js
- **Root Directory:** `./` (пусто)
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию)

### 2.3 Настройка Environment Variables

В разделе "Environment Variables" добавьте все переменные:

#### Обязательные переменные:

```bash
# Database (PostgreSQL - Neon)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# NextAuth
NEXTAUTH_SECRET=your-secret-here
# Сгенерируйте: openssl rand -base64 32
NEXTAUTH_URL=https://yourproject.vercel.app

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# OpenAI
OPENAI_API_KEY=sk-...

# TransferMit Payment Gateway
TM_API_KEY=your-transfermit-api-key
TM_SIGNING_KEY=your-transfermit-signing-key

# SMTP Email
SMTP_HOST=mail.privateemail.com
SMTP_PORT=465
SMTP_USER=your-email@domain.com
SMTP_PASS=your-email-password
SMTP_FROM=your-email@domain.com
SMTP_FROM_NAME=Avenqor Support
```

#### Опциональные переменные:

```bash
SITE_BASE_URL=https://yourproject.vercel.app
TM_API_URL=https://app.transfermit.com/api/v1
```

**Важно:**
- Добавьте переменные для всех окружений (Production, Preview, Development)
- Для Production используйте production значения
- `NEXTAUTH_URL` должен совпадать с вашим Vercel доменом

### 2.4 Деплой

1. Нажмите "Deploy"
2. Дождитесь завершения сборки
3. После успешного деплоя, Vercel предоставит URL: `https://yourproject.vercel.app`

## 🔍 Шаг 3: Проверка после деплоя

### 3.1 Проверка работоспособности

1. Откройте ваш Vercel URL
2. Проверьте:
   - ✅ Главная страница загружается
   - ✅ TradingView виджеты работают
   - ✅ Переключение языков работает
   - ✅ Footer и Header отображаются корректно

### 3.2 Проверка логов

В Vercel Dashboard → ваш проект → "Logs":
- Проверьте отсутствие ошибок
- Убедитесь, что Prisma Client сгенерирован
- Проверьте подключение к Neon БД

## 🐛 Решение проблем

### Проблема: Ошибка подключения к БД

**Решение:**
- Проверьте `DATABASE_URL` в Vercel environment variables
- Убедитесь, что Neon база активна
- Проверьте, что connection string содержит `?sslmode=require`

### Проблема: NextAuth не работает

**Решение:**
- Проверьте `NEXTAUTH_SECRET` (должен быть минимум 32 символа)
- Убедитесь, что `NEXTAUTH_URL` совпадает с вашим Vercel доменом
- Проверьте Google OAuth credentials

### Проблема: Prisma Client не найден

**Решение:**
- Убедитесь, что `postinstall` скрипт добавлен в `package.json`
- Проверьте логи билда в Vercel
- При необходимости добавьте `prisma generate` в build command

## 📝 Чеклист перед деплоем

- [ ] Neon БД создана и схема применена (`npm run db:push`)
- [ ] Все environment variables добавлены в Vercel
- [ ] `NEXTAUTH_SECRET` сгенерирован (минимум 32 символа)
- [ ] `NEXTAUTH_URL` установлен на Vercel домен
- [ ] Google OAuth настроен с правильными redirect URIs
- [ ] Git репозиторий подключен к Vercel
- [ ] Проект компилируется без ошибок

## 🔄 Обновление приложения

После каждого изменения в коде:

1. Закоммитьте изменения в Git
2. Запушьте в основную ветку
3. Vercel автоматически задеплоит изменения
4. Проверьте логи на наличие ошибок

---

**Готово!** Ваше приложение должно быть доступно по адресу `https://yourproject.vercel.app`

